$schema: "https://moonrepo.dev/schemas/project.json"

language: "javascript"

tasks:
  serve:
    command: "pnpm exec tsx mock-server/server.ts"
    options:
      runFromWorkspaceRoot: true
  test:
    command: "pnpm exec vitest run --config mock-server/vitest.config.ts"
    options:
      runFromWorkspaceRoot: true
